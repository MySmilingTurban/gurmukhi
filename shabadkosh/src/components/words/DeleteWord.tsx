import React, { Component } from "react";
import { Card } from "react-bootstrap";

export class DeleteWord extends Component {
  render() {
    return <Card>
    <Card.Body>
      <Card.Title>Word with </Card.Title>
    </Card.Body>
  </Card>;
  }
}

export default DeleteWord;
